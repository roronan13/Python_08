[tool.poetry]
name = "PYTHON-08-ex1-PROJECT"
version = "0.0.1"
package-mode = false

[tool.poetry.dependencies]
python = "^3.13.1"
pandas = "^3.0.3"
matplotlib = "^3.10.9"
numpy = "^2.4.6"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
